import styled from "styled-components";
import { PageTitle as Title } from "../ProfilePage/styled";
import { NullArrayText as NullArray } from "../../components/Booking/styled";

import { PaginationBtn as Pagination } from "../ProfilePage/styled";

export const PageTitle = styled(Title)``;

export const NullArrayText = styled(NullArray)``;

export const PaginationBtn = styled(Pagination)`
   margin-top: 64px;
   margin-bottom: 0px;
`;

